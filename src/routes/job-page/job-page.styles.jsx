import styled from 'styled-components';
import { PageContainer } from '../../styles/mixins';

export const JobPageContainer = styled(PageContainer)`
  display: flex;
  flex-direction: column;
  padding: 3rem 2.4rem;
`;
